PHYTON – CUBOS

nome = input(“Qaul o nome do cubo? “)

peças = int(input(“Quantas peças tem o cubo? “)

quantidade de peças =  int(input(“Quantas peças você terá que mover para que a peça de referência chegue à posição desejada?”)

deformidade = int(input(“quantas arestas de diferentes tamanhos o cubo possui? “)

distancia = int(input(“Qual é a distancia da peça de referência até o local desejado, em número de peças? “)

complexidade = int(quantidade de peças * deformidade)

entropia = int(complexidade * distancia * quantidade de peças)

print(complexidade)
print(“A complexidade do cubo {} é {}.format(nome,complexidade)